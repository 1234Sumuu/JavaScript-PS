var name = [3, 6, 2, 7, 1,8,9,23,11,45];
var uniqueName = [];

for(var i=0; i<name.length; i++){
    var element = name[i];
    var index = uniqueName.index;
    if(index ==-1){
        uniqueName.push(element);
    }
}